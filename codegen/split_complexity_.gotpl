{{ define "split_complexity_.gotpl" }}
{{- /* Complexity is computed centrally in the root executableSchema.Complexity
       method, which can access ec.complexity (a root-package type). Per-shard
       complexity handlers are intentionally not emitted: a shard cannot type-
       assert to the root *executionContext, so it has no way to reach the
       ComplexityRoot funcs. */}}
{{ end }}
